import numpy as np

def principal(observada, mascara):
    # Retorna uma matriz propositalmente com dimensões erradas (ex: 2x o tamanho original)
    return np.zeros((observada.shape[0] * 2, observada.shape[1]))